# 开发计划与功能建议

本文档旨在为代理池工具的未来发展提供一份清晰的路线图。

## 核心理念

将工具打造得更**自动化**、**智能化**和**用户友好**。

## 近期功能规划 (Short-term)

### 1. **自动测试新代理**
- **描述**: 在通过“获取代理”或“导入代理”功能添加新代理后，自动触发一次后台的“测试代理”流程。
- **价值**: 简化用户操作，实现“即加即用”，无需手动点击测试按钮。

### 2. **增强的代理信息展示**
- **描述**: 在代理列表中增加更多维度的信息，例如：
    - **响应时间图**: 显示单个代理最近几次的延迟变化，帮助用户判断其稳定性。
    - **成功率**: 记录每个代理的测试成功与失败次数，计算并显示成功率。
- **价值**: 提供更丰富的数据，帮助用户做出更明智的代理选择。

### 3. **自定义代理源**
- **描述**: 允许用户在UI界面上添加或编辑代理源URL。程序将定期从这些URL获取新的代理。
- **价值**: 提高灵活性，让用户可以接入自己信任的或私有的代理发布源。

## 中期功能规划 (Mid-term)

### 1. **智能评分与自动排序**
- **描述**: 引入一个更复杂的代理评分系统，综合考虑延迟、速度、成功率、匿名度等多个因素，为每个代理计算一个综合得分。用户可以根据这个得分进行排序。
- **价值**: 将复杂的代理性能指标简化为一个直观的分数，方便快速筛选出最优代理。

### 2. **配置文件支持**
- **描述**: 引入 `config.json` 或 `config.yaml` 配置文件，用于保存用户的设置，如：
    - 筛选条件
    - 代理源列表
    - 轮换间隔
    - 主题偏好
- **价值**: 实现设置的持久化，用户关闭并重新打开应用后，所有配置将自动加载。

### 3. **系统托盘图标与后台运行**
- **描述**: 增加系统托盘图标，允许应用在关闭主窗口后继续在后台运行，提供持续的代理服务。
- **价值**: 提升应用的可用性，使其更像一个系统级的服务工具。

## 长期愿景 (Long-term Vision)

### 1. **多协议支持扩展**
- **描述**: 除了现有的HTTP/SOCKS5，增加对更多代理协议的支持，如 Shadowsocks, Vmess 等。
- **价值**: 扩大工具的适用范围，满足更多高级用户的需求。

### 2. **插件化架构**
- **描述**: 将核心功能模块化，例如代理获取、测试、评分等，允许开发者通过插件的形式进行扩展。
- **价值**: 建立一个开放的生态系统，鼓励社区贡献，极大地丰富工具的功能。
